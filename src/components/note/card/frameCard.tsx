import React from 'react';
import { CardItem } from '../../../store/data/types';

interface Props {
	card: CardItem;
}

export const FrameCard: React.FC<Props> = (props: Props) => {
	return <div>frameCard</div>;
};
